# Rust callers — `tracing-test-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `tracing-test` | `tracing_test` | 19 | 2 | 2 | 5 |

## `tracing-test`

### Inbound symbols (2 distinct, 19 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `tracing-test`.

#### `tracing_test::internal::MockWriter` — 3 site(s) (3 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/mcp_tool_call_tests.rs:37` — `use tracing_test::internal::MockWriter`
- ✅ `codex-kotlin/codex-core` `src/plugins/discoverable_tests.rs:16` — `use tracing_test::internal::MockWriter`
- ✅ `codex-kotlin/codex-core` `tests/suite/otel.rs:35` — `use tracing_test::internal::MockWriter`

#### `tracing_test::traced_test` — 16 site(s) (2 codex, 14 other, 2 caller crates)

- ✅ `codex-kotlin/codex-core` `tests/suite/client_websockets.rs:52` — `use tracing_test::traced_test`
- ✅ `codex-kotlin/codex-core` `tests/suite/otel.rs:32` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/default_provider/account_id_endpoint_mode.rs:46` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/default_provider/auth_scheme_preference.rs:84` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/default_provider/checksums.rs:85` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/default_provider/disable_request_compression.rs:46` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/default_provider/endpoint_url.rs:69` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/default_provider/ignore_configured_endpoint_urls.rs:47` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/default_provider/request_min_compression_size_bytes.rs:48` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/default_provider/use_dual_stack.rs:44` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/default_provider/use_fips.rs:44` — `use tracing_test::traced_test`
- `aws-config-kotlin/aws-config` `src/ecs.rs:513` — `use tracing_test::traced_test`
- _… and 4 more site(s)._
