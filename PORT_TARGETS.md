# Port targets — `tracing-test-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `tracing-test` that codex-rs uses, and therefore the minimum subset that `tracing-test-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-170] (rank `0`, tier `direct`, status `To Do`)

**Cargo package name**: `tracing-test`
**Rust module name (in `use` statements)**: `tracing_test`

## Summary

- **2** distinct symbols imported by codex-rs
- **5** total `use` statements
- **4** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `tracing_test::internal::MockWriter`

- **3 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/mcp_tool_call_tests.rs:37` (`codex-core`) — `use tracing_test::internal::MockWriter`
- `core/src/plugins/discoverable_tests.rs:16` (`codex-core`) — `use tracing_test::internal::MockWriter`
- `core/tests/suite/otel.rs:35` (`codex-core`) — `use tracing_test::internal::MockWriter`

### `tracing_test::traced_test`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/suite/client_websockets.rs:52` (`codex-core`) — `use tracing_test::traced_test`
- `core/tests/suite/otel.rs:32` (`codex-core`) — `use tracing_test::traced_test`
